kind = "web"
previewPath = "/"
title = "Student Stat Stream"
version = "1.0.0"
id = "artifacts/student-stat"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21370

[services.development]
run = "pnpm --filter @workspace/student-stat run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/student-stat", "run", "build" ]
run = "pnpm --filter @workspace/student-stat run start"

[services.env]
PORT = "21370"
BASE_PATH = "/"
